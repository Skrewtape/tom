angular.module('app.playerselect_player_edit.player_edit.link_division',['app.playerselect_player_edit.player_edit.link_division.process',/*REPLACEMECHILD*/]);
angular.module('app.playerselect_player_edit.player_edit.link_division').controller(
    'app.playerselect_player_edit.player_edit.link_division',
    function($scope, $state, StatusModal, TimeoutResources) {
	//$scope.player_info=$state.params.newPlayerInfo;
	//if($scope.checkForBlankParams($scope.player_info) == true){
	//    return;
	//}
	//$scope.player = TimeoutResources.addPlayerResource().addPlayer(submit_data);	    
    }
);
