angular.module('app.playerselect_player_info',['app.playerselect_player_info.player_info',/*REPLACEMECHILD*/]);
angular.module('app.playerselect_player_info').controller(
    'app.playerselect_player_info',
    function($scope, $state, StatusModal, TimeoutResources) {
	//$scope.player_info=$state.params.newPlayerInfo;
	//if($scope.checkForBlankParams($scope.player_info) == true){
	//    return;
	//}
	//$scope.player = TimeoutResources.addPlayerResource().addPlayer(submit_data);	    
    }
);
