app.controller(
    'home.poop',
    function($scope, $http, $uibModal, $state, $location, Page, StatusModal) {
        Page.set_title('');
	$scope.get_all_params($scope);
        //$scope.back_string = ''
})
